# Current Flag: Full One-Shot Re-Earn

**When:** 2026-08-09  
**Run:** `runs/full-flag-reearn-20260809`  
**Outcome:** FLAG PLANTED

## Route reached

1. Vanilla original returned `[1, 3, 4, 5, 2]`.
2. Vanilla changed wording returned `[5, 4, 2, 1, 3]`.
3. The Niodoo teaching process started from an empty store.
4. The model wrote two durable rule entries without an operator save command.
5. The store contained the family rule and walk-from-end scar, not the numeric destination.
6. The teaching process exited.
7. Two new wording-only processes started from the self-saved store.

## Restored arming

The historical stderr showed God Zone goal force `4.5`; without God Zone, fresh goal force was about `493.48` and dual force about `0.05`. Restoring `NIODOO_GOD_ZONE_RECOVERY=1` produced:

- `dual_stream_force=26.061428`
- `dual_stream_n_ear=16`
- `dual_stream_survivors=10`
- all three required `REMEMBER_EAR` seed lines present

## Arrival

- Empty store became the byte-identical two-entry family-rule / walk-from-end scar.
- Teaching process exited before transfer.
- Restart 1: `PASS_CONSTRAINED`; oracle gate `OK`.
- Restart 2: `PASS_CONSTRAINED`; oracle gate `OK`.
- Complete stdout, stderr, and `llama.cpp` conversation files remain under the run directory without assistant slicing.

## Recovered route decision

A transfer-only diagnostic set residual beta to the historical telemetry value `1.0`. It reproduced first-token force `26.137074`, then returned `PASS_CONSTRAINED ×2` and oracle gate `OK ×2` from the frozen store. Beta `1.0` is now recorded as a recovered missing route setting. The complete five-phase re-earn then confirmed that setting on the full route.

## Decision

Plant the 2026-08-09 flag. Keep the earlier failed coordinates visible as the climb that recovered God Zone recovery and residual beta `1.0`.
