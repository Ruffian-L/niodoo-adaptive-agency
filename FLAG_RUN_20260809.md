# Flag Run: Full One-Shot Re-Earn

**Who and when:** Jason Van Pham with GitHub Copilot, 2026-08-09  
**Destination:** REACHED  
**Full local run:** `runs/full-flag-reearn-20260809`

## 1. Starting question

Can the complete route run continuously from vanilla control through empty-store teaching, autonomous durable save, process death, and two exact wording-transfer restarts without gold-install paths?

## 2. Route

1. Four vanilla `llama.cpp` conversations.
2. One-token residual arming smoke.
3. Seven-turn Niodoo teaching process from an empty store.
4. Autonomous two-entry durable scar.
5. Teaching process exit.
6. Two new wording-only Niodoo processes using that scar.
7. Authoritative scorer and oracle gate after each restart.

Historical control note: these four archived vanilla conversations used no system prompt. The later matched-system live-runner change does not alter this run or its artifacts.

## 3. How it ran

```bash
cd /home/ruffianl/projects/niodoo-adaptive-agency
ONE_SHOT_OUT="$PWD/runs/full-flag-reearn-20260809" cargo one-shot
```

The runner teed every subprocess byte to the terminal and complete files. It did not extract or shorten assistant text.

## 4. Destination written before the run

Correct answer: `[5, 4, 3, 2, 1, 5]`. Both restarts had to return `PASS_CONSTRAINED`, banned words `[]`, and oracle gate OK. One miss meant no flag.

## 5. Arrival

| Restart | Correct answer | Actual status | Banned | Oracle gate |
|---|---|---|---|---|
| 1 | `[5, 4, 3, 2, 1, 5]` | `PASS_CONSTRAINED`, exact true | `[]` | OK |
| 2 | `[5, 4, 3, 2, 1, 5]` | `PASS_CONSTRAINED`, exact true | `[]` | OK |

The self-saved store contained the family rule and walk-from-end scar, not the numeric destination.

## 6. Climb

| Coordinate or milestone | Outcome |
|---|---|
| Fresh route without God Zone recovery | Dual force about `0.056`; stopped |
| Restore God Zone recovery | Dual force `26.061428`; exact scorer still failed |
| Compare historical and fresh telemetry | Historical beta `1.0`; fresh beta `1.1` |
| Frozen-store beta `1.0` diagnostic | `PASS_CONSTRAINED ×2`; gate OK ×2 |
| Full five-phase route with recovered beta | FLAG reached |

## 7. Math in plain words

Two of two fresh post-death restarts reached the exact destination. This shows the route held twice in this run; it is not a broad reliability estimate.

## 8. Decision

Residual beta `1.0` was recovered from historical telemetry after the fresh default `1.1` repeatedly omitted the final repeated item. It is now an explicit frozen setting. No soft turn, structural phrase pin, prompt reinjection, procedure force emission, order boost, progress digit tip, or surface gold path was enabled.

## 9. Human map check

- [ ] Read all complete control and Niodoo streams.
- [x] Recomputed both score files (2026-08-09).
- [x] Recomputed both oracle gates (2026-08-09).
- [x] Confirmed the self-saved store has no numeric destination (2026-08-09).
- Initials/date: ____________________
