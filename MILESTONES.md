# Milestones

## Near-destination geometry

Boost: dual inject 1.5 and posture 12.  
Coordinate: `[5, 4, 3, 2, 2, 5]`.  
Refuel: the end-to-start walk held almost completely, locating the remaining instability at the penultimate item.

## Procedure geometry located

Boost removed: procedure clauses off.  
Coordinate: `[2, 3, 1, 4, 5]`.  
Refuel: losing the walk showed that the procedure clauses carried useful direction.

## Softer dual force

Boost: inject reduced to 1.0 and posture reduced to 8.  
Coordinate: exact destination `[5, 4, 3, 2, 1, 5]`.  
Refuel: softer force let the scar geometry finish the route without direct next-digit guidance.

## Flag held twice

Two new processes reached the same exact destination from the same frozen store. The second arrival turned a single hit into a visible destination for later explorers.

## Full-loop runner reaches self-save

On 2026-08-09 the one-shot began with vanilla controls, started a seven-turn Niodoo teaching process from an empty store, recorded two autonomous durable writes, and confirmed process death. This is a real milestone even though the following transfer coordinates missed and no new flag was planted.

## Residual arming restored

God Zone recovery normalized the scar goal force and moved first-token dual force from about `0.056` to `26.061428`, with 16 ears and 10 survivors. The full parser-free route could proceed. Both restarts kept clean oracle gates but missed the exact scorer, so this is a milestone rather than a new flag.

## Historical beta recovered

Historical telemetry carried residual beta `1.0` while fresh defaults used `1.1`. A frozen-store diagnostic with beta `1.0` reproduced force `26.137074`, `PASS_CONSTRAINED ×2`, and oracle gate OK ×2. This recovered decision unlocks one complete five-phase re-earn; it does not by itself plant a new flag.

## Full flag re-earned

The 2026-08-09 parser-free one-shot completed the entire route: controls, arming smoke, empty-store teaching, autonomous two-entry scar, process death, and two fresh wording-only restarts. Both authoritative scorers returned `PASS_CONSTRAINED`; both oracle gates returned OK. FLAG planted.
